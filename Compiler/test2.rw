#import io
/* This is a test file designed to test out the lexer! */

fahrenheit_to_celsius(fahrenheit: float) -> float {
    return (fahrenheit - 32) * 5 / 9
}/*
/*
/* */ */ */// Still a comment!!!

celsius_to_fahrenheit(celcius: float) -> float {
    return (celsius * 9 / 5) + 32.0
}

main() -> int {
    auto temp = input_float()
    auto c_or_f = input_char()
    float result
    char result_type
    if c_or_f == 'C' {
        result = celsius_to_fahrenheit(temp)
        result_type = 'F'
    } else if c_or_f == 'F' {
        result = fahrenheit_to_celsius(temp)
        result_type = 'C'
        test := '\n'
    } else {
        print("Input is not 'C' or 'F'. Unable to convert!")
        return 1
    }

    a = 12

    1234jdfhsdf <- this is an error

    println("Converted temperature: {0}⁰{1}", result, result_type)
    return 0;
}
